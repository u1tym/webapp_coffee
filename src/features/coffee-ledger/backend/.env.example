# coffee-ledger の backend/.env

# 接続先DB（開発用DB。詳細は rules/13-db.md）
DB_SERVER=localhost
DB_PORT=5432
DB_DATABASE=tstdb
DB_USERNAME=tstuser
DB_PASSWORD=TSTPASS

# 許可するフロントのオリジン（カンマ区切り。* は使わない）
CORS_ORIGINS=http://localhost:5173

# ログローテーション
LOG_MAX_BYTES=1048576
LOG_BACKUP_COUNT=5

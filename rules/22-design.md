# 全体設計作成（日本語）

> 元Cursorルールでの適用範囲: `specs/**/design.md` を編集するとき。UI 詳細、テーブル定義、API 詳細は別ドキュメント

見出しと本文は日本語で書くこと。

`specs/templates/design-template.md` に従うこと。

要件を満たす設計を記述すること。

フロントエンドとバックエンド（起動、モジュール構成、業務ロジック）の観点で整理すること。

画面が無い場合はフロントエンド設計に「画面なし」と書くこと。ファイルは省略しない。

**UI の詳細（画面毎の部品詳細や細かな画面遷移）は書かない。** `ui-design.md`に書く。

**API の詳細（パス、要求、応答、エラー）は書かない。** `api-design.md` に書く。

**テーブル定義と ER 図は書かない。** `db-design.md` に書く。

認証しない（`rules/14-security.md`）。「認証 / 認可」節は残し、「認証しない」と書く。管理パスワード（簡易ロック）がある場合は、その扱いだけを書く。

要件に存在しない機能を追加しないこと。

ユーザが `requirements.md` を承認するまで、このファイルを作成しないこと。
ユーザがこのファイルを承認するまで、`ui-design.md` を作成しないこと。

承認欄は履歴形式とする（日時・状態・変更概要。詳細は `CLAUDE.md`）。

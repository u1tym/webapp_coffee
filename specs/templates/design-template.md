# <機能名> 全体設計

> UI の詳細（画面毎の部品配置・画面遷移）は書かない → `ui-design.md`
> API の詳細（パス・要求・応答・エラー）は書かない → `api-design.md`
> テーブル定義と ER 図は書かない → `db-design.md`

## 全体構成

<機能全体の構成を簡潔に。フロントエンド（SPA）とバックエンド（別プロセスの FastAPI）が疎通する構成であることが前提>

## フロントエンド設計

<画面がある場合は主要な画面と役割を一覧で。画面が無い場合は「画面なし」と書く>

- 起動方法: `src/features/<feature-name>/frontend/` で `npm run dev`
- モジュール構成: <主要なディレクトリ・コンポーネントの役割>

## バックエンド設計

- 起動方法: `src/features/<feature-name>/backend/` で venv を有効化し uvicorn 起動
- モジュール構成: <主要なパッケージ・モジュールの役割>
- 業務ロジック: <主要な処理の流れ>

## 認証 / 認可

<認証しない場合は「認証しない」と書く。認証する場合のみ方式と対象範囲を書く>

## 外部連携

<他システム・外部 API との連携があれば。無ければ「なし」>

## 未決事項

<この時点で決まっていないこと。無ければ「なし」>

## 承認

現在の状態: 未承認

| 日時 | 状態 | 変更概要 |
|------|------|----------|
| | 未承認 | 初版 |

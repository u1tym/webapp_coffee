# <機能名> API設計

> テーブル定義と ER 図は書かない（`db-design.md` を参照）。画面レイアウト・部品配置は書かない（`ui-design.md`）。Vue コンポーネント構成は `design.md`。

## 共通事項

- ベース URL: `VITE_API_<FEATURE>_URL`（フロントの環境変数。詳細は `rules/11-frontend.md`）
- 認証: <この機能の API 全体で認証する場合は方式を書く。認証しない場合は「認証しない」>
- 共通エラー

| HTTP ステータス | 状況 |
|------|------|
| 401 | 未ログイン（認証する場合のみ） |
| 403 | 権限なし（認証する場合のみ） |
| 422 | 入力不正 |
| 500 | サーバ内部エラー |

## エンドポイント一覧

| メソッド | パス | 認証 | 概要 |
|----------|------|------|------|
| GET | /items | 要 | |

## エンドポイント詳細

### GET /items

- **認証**: 要 / 不要
- **要求**

| 項目 | 型 | 必須 | 説明 |
|------|----|----|------|
| | | | |

- **応答（200）**

```json
{
}
```

- **エラー**

| HTTP ステータス | 条件 |
|------|------|
| | |

## 承認

現在の状態: 未承認

| 日時 | 状態 | 変更概要 |
|------|------|----------|
| | 未承認 | 初版 |
